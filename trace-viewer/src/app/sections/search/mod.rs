//! Implements the [Section] which enables the user to define search parameters and initiate a search.
mod context;
mod search_control;
mod search_section;
mod search_settings;

pub(crate) use context::SearchLevelContext;
pub(crate) use search_section::SearchSection;
